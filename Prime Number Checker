import math
def is_prime(num):
    if(num<=1):
        print("It's not prime")
    else:
        for i in range(2,int(math.sqrt(num))+1):
            if(num%i==0):
                return False
            
        return True
is_prime(89)
            
